# Collaboration Platform Frontend

Vite, React, and TypeScript frontend for the multi-tenant collaboration platform.

## Commands

```powershell
npm install
npm run dev
npm run typecheck
npm run lint
npm run build
npm test
```

Copy `.env.example` to `.env.local` and set `VITE_API_BASE_URL` for local API access.
